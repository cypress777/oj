#include <iostream>
#include <vector>
#include <bitset>

using namespace std;

int SS = 16;
int LEN = 135;

bitset<LEN> setbit(bitset<LEN> bits, const vector<int> &xs, const vector<int> &ys) {

}

int main() {
    int N;
    cin >> N;

    vector<bitset<LEN>> modes(N, bitset<LEN>());
    for (int i = 0; i < N; i++) {
        int t;
        cin >> t;

        vector<int> xs, ys;
        for (int i = 0; i < t; i++) {

        }
    }


    return 0;
}

